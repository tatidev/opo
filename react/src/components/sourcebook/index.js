import React from 'react';
import ReactDOM from 'react-dom';
import {SourceBook} from "./SourceBook";
let container = document.getElementById('ReactDOMContainer');
ReactDOM.render(<SourceBook />, container);